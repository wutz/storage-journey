# 单包项目：显式声明 packages，兼容要求该字段的旧版 pnpm（如 Cloudflare 构建镜像）
packages:
  - .

# 允许执行安装脚本的依赖：pnpm 11+ 读 allowBuilds，pnpm 10 读 onlyBuiltDependencies
allowBuilds:
  esbuild: true
  workerd: true
onlyBuiltDependencies:
  - esbuild
  - workerd
